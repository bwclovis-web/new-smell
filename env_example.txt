# Database Configuration
# Copy this file to .env and fill in your actual values

# Primary Database URL (used by Prisma)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Local Development Database (fallback)
LOCAL_DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Vercel Environment Variables
# Add these in your Vercel project settings:
# DATABASE_URL=your_production_database_url

# CrewAI Configuration
# OpenAI API Key (required for CrewAI)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize the LLM model
# OPENAI_MODEL=gpt-4o-mini

# Optional: Set custom rate limiting (in seconds)
# MIN_REQUEST_DELAY=2

# Optional: Set output directory
# OUTPUT_DIR=enriched_data

# Optional: Set logging level
# LOG_LEVEL=INFO

# Session Secret (required for authentication)
SESSION_SECRET=your_session_secret_here

# JWT Secret (required for authentication - minimum 32 characters)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long

# Node Environment
NODE_ENV=development
