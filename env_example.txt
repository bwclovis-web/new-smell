# Database Configuration
# Copy this file to .env and fill in your actual values

# Primary Database URL (used by Prisma) - REQUIRED
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Local Development Database (fallback) - OPTIONAL
LOCAL_DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Vercel Environment Variables
# Add these in your Vercel project settings:
# DATABASE_URL=your_production_database_url

# Authentication Secrets - REQUIRED
# Session Secret (minimum 32 characters, 64+ recommended for production)
SESSION_SECRET=your_session_secret_here_minimum_32_characters_long

# JWT Secret (minimum 32 characters, 64+ recommended for production)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long

# Node Environment - REQUIRED
NODE_ENV=development

# Server Configuration - OPTIONAL
# APP_PORT=2112
# METRICS_PORT=3030

# AI/ML Services - OPTIONAL
# OpenAI API Key (required for CrewAI features)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize the LLM model
# OPENAI_MODEL=gpt-4o-mini

# Security Features - OPTIONAL
# Honeypot encryption seed (minimum 16 characters)
# HONEYPOT_ENCRYPTION_SEED=your_honeypot_seed_here

# Redis Configuration - OPTIONAL
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# Monitoring - OPTIONAL
# MONITORING_ENABLED=true

# Testing - OPTIONAL
# TEST_DATABASE_URL=postgresql://username:password@localhost:5432/test_database
# CI=false

# CrewAI Configuration - OPTIONAL
# MIN_REQUEST_DELAY=2000
# OUTPUT_DIR=enriched_data
# LOG_LEVEL=INFO
# MAX_RETRIES=3
# TIMEOUT=30000
